# Copy this file to .env and paste your browser Maps API key.
# Never commit .env (it is gitignored).
#
# Google Cloud: enable "Maps JavaScript API" + "Directions API",
# restrict the key by HTTP referrer (localhost + your Vercel URL).

GOOGLE_MAPS_API_KEY=
