/**
 * Google Maps JavaScript API key (browser / restricted key).
 * Enable: Maps JavaScript API + Directions API. Restrict key by HTTP referrer (e.g. http://localhost:8765/*).
 * Leave empty to skip live km — the app still shows “Open in Google Maps” links from route-overlays.json.
 */
window.__GOOGLE_MAPS_API_KEY__ = "";
