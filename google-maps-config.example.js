/**
 * Copy this file to google-maps-config.js and set your key.
 * Google Cloud Console: enable “Maps JavaScript API” and “Directions API” for the same project.
 * Create a key → Application restrictions → HTTP referrers → add http://localhost:8765/* and your production origin.
 */
window.__GOOGLE_MAPS_API_KEY__ = "YOUR_BROWSER_KEY_HERE";
